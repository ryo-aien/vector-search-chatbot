version: "3.9"

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # ChromaDBデータを永続化
      - chroma_data:/app/data/chroma
    restart: unless-stopped

volumes:
  chroma_data:
